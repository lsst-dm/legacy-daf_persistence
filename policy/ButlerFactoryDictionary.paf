#<?cfg paf dictionary ?>

target: ButlerFactory

definitions: {
    mapperName: {
        type: string
        description: "The name of the mapper Python class."
        maxOccurs: 1
    }
    mapperPolicy: {
        type: policy
        # description: "An optional policy for the mapper."
        maxOccurs: 1
    }
    persistencePolicy: {
        type: policy
        # description: "An optional policy to configure persistence."
        maxOccurs: 1
    }
}
